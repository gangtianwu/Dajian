<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">
    <RelativeLayout
        android:background="@color/nodata_background"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        >
        <ImageView
            android:layout_centerVertical="true"
            android:id="@+id/car_activity_iv"
            android:src="@mipmap/back"
            android:layout_marginLeft="20dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            />
        <TextView
            android:layout_centerHorizontal="true"
            android:gravity="center"
            android:id="@+id/tv_item"
            android:textSize="16sp"
            android:text="基本信息"
            android:layout_width="wrap_content"
            android:layout_height="match_parent" />
    </RelativeLayout>

    <android.support.design.widget.TabLayout
        android:background="@color/cardview_light_background"
        android:id="@+id/car_activity_tablayout"
        app:tabIndicatorColor="#008AFF"
        app:tabIndicatorHeight="4dp"
        app:tabSelectedTextColor="#008AFF"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar">

    </android.support.design.widget.TabLayout>
    <android.support.v4.view.ViewPager
        android:id="@+id/viewpager"
        android:layout_width="match_parent"
        android:layout_height="5dp"
        android:layout_above="@+id/rg"
        android:layout_below="@+id/car_activity_tablayout"
        android:layout_weight="1">

    </android.support.v4.view.ViewPager>

</LinearLayout>